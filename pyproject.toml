[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[project]
name = "mixing"
version = "0.0.52"
description = "Tools for video and audio editing"
readme = "README.md"
requires-python = ">=3.10"
keywords = []
authors = [
    { name = "Thor Whalen" },
]
dependencies = [
    "moviepy",
    "config2py",
    "dol",
    "xdol",
    # opencv-contrib-python (superset of opencv-python; MIT/Apache) is the single cv2 provider
    # across the ecosystem — mediapipe + syncnet require it, so using it here avoids the
    # unsupported double-install (both provide `cv2`) when muvid[scoring] is co-installed.
    "opencv-contrib-python",
    "pillow",
    "pydub",
    "numpy",
    # scipy backs mixing.audio's cross-correlation alignment (find_audio_offset /
    # find_audio_offset_detailed / align_clips_to_reference). It is imported lazily
    # (require_package("scipy.signal")) so `import mixing` stays light, but it must be
    # installed for alignment to run — a core mixing.audio capability, so base deps
    # (documented as a mixing.audio dep in .claude/CLAUDE.md).
    "scipy",
    "burns",
    # The geometry VOCABULARY (SOCIAL_SIZES, the fit/fill/stretch mode names,
    # the social backdrop's blur sigma and dim factor) is owned by `looks` and
    # imported by mixing.video.video_util — one place says what a "shorts" is.
    # The implementations stay here: video_util resizes with moviepy, and the
    # `social` branch is a composite, not a formula. `looks` declares no
    # dependencies of its own, so this edge costs nothing. The floor is the
    # first release carrying SOCIAL_SIZES / FitMode / DFLT_BACKDROP_*.
    "looks>=0.0.11",
]

[project.license]
text = "MIT"

[project.urls]
Homepage = "https://github.com/thorwhalen/mixing"

[project.optional-dependencies]
testing = [
    "pytest",
]
clipboard = [
    "pyclip",
]
audio = [
    "pydub",
    "ffmpeg-python",
    "soundfile",
]
# Beat / downbeat / onset analysis (mixing.audio.beat_grid). librosa is ISC
# (commercial-clean) and lazy-imported, so `import mixing.audio` stays light.
beats = [
    "librosa",
]
widget = [
    "ipywidgets",
    "soundfile",
    "jupyter",
]
gen = [
    "google-genai",
]
llm = [
    "aix",
]
dev = [
    "pytest",
    "ruff",
]

[tool.ruff]
line-length = 88
target-version = "py310"
exclude = [
    "**/*.ipynb",
    ".git",
    ".venv",
    "build",
    "dist",
    "tests",
    "examples",
    "scrap",
]

[tool.ruff.lint]
select = [
    "D100",
]
ignore = [
    "D203",
    "E501",
    "B905",
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
"**/tests/*" = [
    "D",
]
"**/examples/*" = [
    "D",
]
"**/scrap/*" = [
    "D",
]

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = [
    "mixing",
]
# EXACTLY what wads' run-tests action passes with `-o`, so a bare `pytest`
# runs what CI runs. NORMALIZE_WHITESPACE was here and is NOT there, so a
# doctest whose expected output was wrapped across lines passed locally and
# failed in CI — verified on this branch before the flags were aligned. No
# doctest in the package relies on it (checked: the suite is green either way).
doctest_optionflags = [
    "ELLIPSIS",
    "IGNORE_EXCEPTION_DETAIL",
]

[tool.wads.ops.ffmpeg]
description = "Multimedia framework for video/audio processing"
url = "https://ffmpeg.org/"
check.linux = "which ffmpeg"
check.macos = "which ffmpeg"
check.windows = "which ffmpeg"
install.linux = ["sudo apt-get update", "sudo apt-get install -y ffmpeg"]
install.macos = "brew install ffmpeg"
install.windows = "choco install ffmpeg -y --no-progress"

[tool.wads.ci]
project_name = ""

[tool.wads.ci.commands]
pre_test = []
test = []
post_test = []
lint = []
format = []

[tool.wads.ci.env]
required_envvars = []
test_envvars = []
extra_envvars = []

[tool.wads.ci.env.defaults]

[tool.wads.ci.quality.ruff]
enabled = true

[tool.wads.ci.quality.black]
enabled = false

[tool.wads.ci.quality.mypy]
enabled = false

[tool.wads.ci.testing]
python_versions = [
    "3.10",
    "3.12",
]
pytest_args = [
    "-v",
    "--tb=short",
]
coverage_enabled = true
coverage_threshold = 0
coverage_report_format = [
    "term",
    "xml",
]
exclude_paths = [
    "examples",
    "scrap",
]
test_on_windows = true

[tool.wads.ci.metrics]
enabled = true
config_path = ".github/umpyre-config.yml"
storage_branch = "code-metrics"
python_version = "3.10"
force_run = false

[tool.wads.ci.build]
sdist = true
wheel = true

[tool.wads.ci.publish]
enabled = true

[tool.wads.ci.docs]
enabled = true
builder = "epythet"
ignore_paths = [
    "tests/",
    "scrap/",
    "examples/",
]
